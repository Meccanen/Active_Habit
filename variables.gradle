// android/app/variables.gradle (referans)
ext {
    // minSdkVersion 24 (Android 7.0): geniş cihaz desteği.
    minSdkVersion    = 24
    compileSdkVersion= 36
    targetSdkVersion = 36

    // Google Play'in istediği sürüm artışları CI workflow'da otomatik yapılır.
    versionCode = 1
    versionName = "1.0.0"
}
